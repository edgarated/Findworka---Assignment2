import React from 'react';

const Button = (props) => {

    return (
      <div>
      <Button onClick={props.onClick} type="button">Submit</Button>
      
      
      </div>


    );
}

export default Button;